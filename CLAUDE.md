# Affina Space

Virtual incubator for early-stage female founders. React + Vite + TS + Tailwind SPA (react-router), Vercel Hobby, Neon Postgres + Drizzle in `/api`. Prod: https://affina-space.vercel.app

## Design — read this before any UI work
The visual design system is defined in **[DESIGN.md](./DESIGN.md)**. Tokens are wired into `tailwind.config.js` and `src/index.css`.

Use the Tailwind classes (`bg-brand`, `text-ink`, `bg-canvas`, `rounded-card`, `rounded-control`, `rounded-pill`, `font-display`, `font-sans`, `bg-brand-50`, `.type-tag`, etc.). **Never** reintroduce Inter or Fraunces, pure-white (`#fff`) page backgrounds, pure-black (`#000`) text (use `ink` `#0B0A08`), or the old violet (`#7150EA` / `#9333EA`).

Brand in one line (design-system v2, 2026-07): **confident, editorial, tactile — one font Archivo (heavy display 900 / condensed-uppercase tags), royal-purple primary (`#6D28D9`) + emerald accent (`#0F9D74`) on a warm bone canvas (`#ECE9E2`), near-black ink (`#0B0A08`), 16px cards / 8px controls, soft warm shadows + faint grain.**

## Product spec — source of truth
- **SPEC_PROGRAM_V2.md** — the program (13 modules M0–M12, block kinds, Snapshot, Delegate, Briefing/Debrief, Launch Readiness §7, mentor sessions). Implemented in full (Phases 1–3 + audit P1–P5).
- **SPEC_M0_INTAKE_REDESIGN.md** — M0 intake (implemented: m0l3 five-field review + links, m0l4 Typeform quiz, m0l5 approve CTA).
- **RULES_DONE_FOR_YOU.md** — Market Research (m2l6, live in test mode "model estimates only") + Delegate modes A/B/C map (§2.2–2.4).
- **RUBRICS_M0-M4.md / RUBRICS_M5-M12.md** — per-block AI-review rubrics → generated into `src/rubrics.ts` (44 rubrics + global rules). Edit the MD, regenerate.
- **CONTENT_*.md** — final lecture texts. All "Coming soon" stubs are filled; 12 `// TODO: rewrite per spec` blocks in data.ts still have old text while their REWRITE versions sit ready in CONTENT_M5-M12.md.

**ID numbering:** all working docs (CONTENT_*, RUBRICS_*, RULES_DONE_FOR_YOU) were normalized to the live `src/data.ts` numbering on 2026-07-03 — ids now match 1:1 (retired old blocks appear as `old-mXlY` markers). The only doc still in pre-restructure numbering is the historical SPEC_PROGRAM_V2.md (flagged in its header) — for it, match by block title.

## Dev workflow & constraints (hard-won, follow these)
- **Deploy:** `npx vercel build --prod` → `npx vercel deploy --prebuilt --prod` → smoke via `/usr/bin/curl` (homepage 200, `/api/progress?email=…` returns JSON — SPA rewrite must not shadow `/api`). Vercel preview URLs are SSO-gated (unusable); test on prod.
- **Vercel Hobby limits:** max **12 serverless functions** — every `.ts` under `/api` counts; shared non-handler code goes in `src/server/` (e.g. `src/rubrics.ts`, `src/server/*`). **Currently 12/12 — FULL** (`api/lib/` retired — progressUtils→`src/server/`; pulse consolidated to one `api/pulse.ts`; plus `api/auth.ts`, `api/cron.ts`, `api/stripe.ts`, `api/admin.ts`). **No free slots left** — new server logic must fold into an existing handler via action/mode routing, never a new file. `maxDuration: 60` is set in vercel.json — long AI generations must fit ~50s (trim max_tokens/verbosity, e.g. market research is budgeted to ~2300 tokens).
- **DB migrations:** use the versioned runner — add `migrations/NNNN_desc.sql` (idempotent: `ALTER TABLE … ADD COLUMN IF NOT EXISTS`, `CREATE … IF NOT EXISTS`), update `src/db/schema.ts` in the same change, then `npm run migrate` (headless, no TTY; tracked in `_migrations` with checksums; safe to re-run). See `migrations/README.md`. `drizzle-kit push` still fails headless — don't use it. The old `scripts/add-*.mjs` are superseded by `migrations/0000_baseline.sql`.
- **Secrets:** keys live in `.env.local` only (git-ignored via `*.local`); Vercel env vars via Dashboard. Never print key values.
- **Test users:** create via `POST /api/user` with `freshStart: true`; walk lessons via `POST /api/brain {action:'toggle-complete'}`; program tasks auto-sync on `GET /api/tasks`. Always delete test users after (`DELETE FROM users WHERE email LIKE 'audit-%@test.local'` + `ALTER SEQUENCE users_id_seq RESTART WITH 1`) — cascades clean everything.
- **AI endpoints pattern:** action/mode routing inside existing handlers (brain.ts: snapshot/market-research; ai.ts: feedback/compare/delegate/generate-name; tasks: briefing/debrief) — never a new function file. Zod-validate LLM JSON leniently (`z.coerce`, `.catch()`); non-JSON answers may be a legit "ask one question" path. **Models are centralized:** `src/server/models.ts` (`MODELS.standard`=`claude-sonnet-5`) + every call routes through `callClaude(params, meta)` in `src/server/anthropic.ts` (shared client + per-call usage logging) — no inline model strings (`grep claude- api/` = 0), no per-file `new Anthropic`. `callClaude` defaults `thinking:{type:'disabled'}` (Sonnet 5 runs adaptive otherwise); pass your own `thinking` to override.
- **Verification:** browser preview tooling does not work in this environment — verify with `tsc -b` + `vite build` + live curl checks against prod APIs; UI interactions get eyeballed by Shamil.
- Commits in English with `Co-Authored-By: Claude <model> <noreply@anthropic.com>`; commit per logical task; deploy after each user-visible change.

## Current state (2026-07-04)
Program v2 fully live, ALL lecture texts filled (M0–M12, LECTURES_V2 markup via LessonBody renderer). Paywall LIVE (SPEC_PAYWALL): m4l10 "Founder's Case" reveal (ambition-calibrated to m0l4 quiz goal3y) → dismissible /unlock (users.subscribed; "Unlock" is a stub flipping the flag, Stripe drops in later) gating M5–M12 → full-page /start-session S1 booking. Also: M0 intake (6-Q quiz incl goal3y), m4l5 three-block For/Against/Conclusion, Traction card deterministic Business tiers (+ red staleness card), Interview Log Add-Transcript, Tasks Real World/Practice + newest-first, course-level pills, Snapshot, Delegate A/B/C, 44 rubrics, mentor S1–S3, Market Research test-mode. DB wiped for launch.


## Backlog (agreed, not started)
- Stripe SHIPPED (SPEC_STRIPE): real subscription replaces the paywall stub. Model = €360 first 3 months upfront → €1,200/yr, via **Stripe Subscription Schedules** (phase 1 quarterly ×1 cycle → phase 2 annual ongoing). One new fn **`api/stripe.ts` (now 12/12)**, action-routed via `?action=`: **checkout** (session-authed, `client_reference_id`=user.id, redirects to hosted Checkout) · **portal** (Customer Portal) · **webhook** (Stripe-signed). Webhook needs the RAW body → `export const config = { api: { bodyParser: false } }` + manual stream read; branch on the `stripe-signature` header. **`subscribed` flips ONLY in the webhook** (`checkout.session.completed`→true + sets up the phase-2 schedule + stores ids + subscription email; `invoice.paid`→keep true; `customer.subscription.deleted`→false; `invoice.payment_failed`→`past_due`, keep access til period end §6). Browser PATCH `subscribed` REMOVED (user.ts). Gating M5–M12 unchanged (still `users.subscribed`). Data: `users.stripe_customer_id / stripe_subscription_id / subscription_status / current_period_end` (`scripts/add-stripe-cols.mjs`). Frontend: Paywall → `POST /api/stripe?action=checkout` → redirect; `/unlock/success` (`PaymentSuccess`) POLLS the server for `subscribed` (never trusts the redirect) → S1 booking; AccountPanel "Manage subscription" → portal. Helper `src/server/stripe.ts` (client + price IDs, no apiVersion pinned). **The 2-phase transition (quarterly→annual after month 3) was VERIFIED against Stripe's real test API with a Test Clock** (advanced 95 days → subscription on the annual price). Note: current Stripe API (`2026-06-24.dahlia`) dropped phase `iterations` — use the from_subscription phase[0] end_date + append annual; `current_period_end` is on the item, not the subscription (helper handles both). Prod smoke: checkout/portal no-cookie→401, webhook branch reached, portal-no-customer→400. ⚠️ **Manual (Shamil) — prod checkout returns 503 until done:** add `STRIPE_SECRET_KEY`, `STRIPE_PRICE_QUARTERLY` (`price_1TppacH9BgajPg4t9az1fZMc`), `STRIPE_PRICE_ANNUAL` (`price_1TppawH9BgajPg4tQatnBc0d`) to Vercel; register the webhook (`https://affina-space.vercel.app/api/stripe`) → put its secret in `STRIPE_WEBHOOK_SECRET`; test with `4242…`; then swap test→live keys (§10). Billing-period decision from SPEC_PAYWALL §6 is now resolved (3mo→annual).
- Auth Phase A SHIPPED (SPEC_RESEND_AUTH): Resend email layer (`src/server/email.ts`, fire-and-forget) + magic-link login (`api/auth.ts` request-link/verify-link, `auth_tokens` table, signed httpOnly session cookie `src/server/session.ts`). Real /login + /auth/verify; welcome + subscription emails wired. §1 freed slots first (progressUtils→src/server, pulse 3→1). **Auth Phase B SHIPPED (SPEC_AUTH_PHASE_B):** session enforcement — identity comes ONLY from the signed httpOnly cookie; the client `?email=` is retired. `requireAuth` (`src/server/requireAuth.ts`) guards all 9 authed endpoints (user GET/PATCH, progress GET, brain, tasks index+submit, pulse, northstar, ai except `generate-name`); no/bad cookie → 401. Ownership (§4): nested ids scoped to the session user (tasks by userId). Pre-auth boundary (§7), kept explicitly separate: `api/user.ts` **POST = onboarding pending-write only** (IP-limited, refuses any verified row — no overwrite/leak; emailCapture keeps §2a), `api/score.ts` + ai `generate-name` = pure-compute onboarding calls (IP-limited), auth.ts/cron.ts unchanged, progress POST stays MENTOR_SECRET. Rate limiter (§6) keys the email dimension on the SESSION email (untrusted body/query email removed); pre-auth = IP only. Frontend: all `?email=` gone; post-auth writes drop body email (new `patchUserToDB` PATCHes profile via the cookie); global 401 interceptor → clear state + `/login`; real logout (`POST /api/auth {action:'logout'}` → clearSession); **"sign in" (Welcome / email-capture block) routes to `/login`** — a magic link is the ONLY auth (typing an email no longer loads an account). Verified on prod 17/17 (incl. `?email=other`+valid session → SESSION user's data; A→B task submit → 404). All email/auth env vars confirmed in Vercel (a forged-cookie smoke authenticated on prod → SESSION_SECRET matches). **Still open:** `api/stripe.ts` (uses a freed slot).
- Email catalog SHIPPED (SPEC_EMAILS — source of truth for copy/triggers): all 8 emails in `src/server/email.ts`. Transactional: magic-link, welcome, subscription, mentor-booked (fires on `booked` flip in user.ts PATCH). Lifecycle cron `api/cron.ts` (CRON_SECRET-gated) sends each email at the user's LOCAL 11:00 (`users.timezone`, browser-captured at onboarding; existing users default UTC): Thu weekly-tasks (if open tasks), Sat reflection, session-due→book-mentor, 14d-inactive→re-engagement. Driven HOURLY by GitHub Actions (`.github/workflows/lifecycle-cron.yml`; Vercel Hobby caps cron at daily — Vercel `crons` removed) — the endpoint's per-user hour-gate means hourly runs nudge everyone at their own 11:00; fallback = cron-job.org. Dedup via `email_log` (`src/server/emailLog.ts` `sendOnce`); `users.lastActiveAt` activity signal (progress GET). Onboarding collects `users.country`/`city`/`timezone` (location step). Cron test hooks: `?day=N`/`?hour=N`/`?dry=1`/`?only=<email>`. All email/auth env vars in Vercel (incl. CRON_SECRET); CRON_SECRET also a GitHub Actions repo secret. Broadcasts (§5) = Resend dashboard, no code. **AMENDMENT (SPEC_EMAILS §A–E):** `users.verifiedAt` (null=pending). Welcome fires on first magic-link verification (auth.ts verify-link), NOT signup (removed from user.ts). Cron branches on state: PENDING (unverified) → finish-registration chain (#9/#10/#11 by age since createdAt +1d/+3d/+7d, each once via email_log `finish_1/3/7`, CTA=fresh magic link from `src/server/magicLink.ts`, then stop; verifying stops it); REGISTERED → #5–#8. #5–#8 incl re-engagement never send to pending. #10 copy = source-verified 5×/SCORE (SPEC §D — "with the right support, 5× more likely to start", SBA/SCORE).
- API hardening SHIPPED (SPEC_API_HARDENING.md): CORS allowlist (`ALLOWED_ORIGINS`, `applyCors` in src/server/http.ts) on all 11 handlers + Neon rate limiter (src/server/ratelimit.ts, `rate_limits` table, IP 20/min+300/day, email 200/day) on the 7 Claude endpoints → 429 + Retry-After, fails open. Helpers in src/ (not api/lib — 12-fn cap). ⚠️ Manual: set Anthropic monthly spend cap (Console → Billing) + `ALLOWED_ORIGINS` in Vercel env.
- Model strategy SHIPPED (SPEC_MODEL_STRATEGY.md): central `src/server/models.ts` + `callClaude` wrapper (usage logging). All 16 calls on Sonnet 5; name-gen moved Haiku→Sonnet 5 (max_tokens 32). Prompt caching wired on feedback/compare but DORMANT — every stable prefix is 125–1289 tok, below Sonnet 5's ~2048 cache min, so `cache_read` stays 0 until prompts grow. Open decision: promote brain snapshot + founders-case to `deep`/Opus 4.8 (recommended in §3 map, left on standard).
- Onboarding funnel SHIPPED (SPEC_ONBOARDING_FUNNEL — email-first + magic-link account). New order: intake+location → **email capture** (before the report, `EmailCapture.tsx`, +consent) → analyzing → report (`RevealTeaser`, CTA "Start the program for free") → **name** (`OnboardingName.tsx`) → project name → **confirm-email** (`ConfirmEmail.tsx`: prefilled, change-email, "Send me a magic link", +consent) → click link → welcome zone. Old `Register/ProgramIntro/Unlock` screens deleted. Email capture creates the PENDING user + `users.emailCapturedAt` (starts the finish clock) and persists the report on `users.onboardingReport` (jsonb); §2a ownership — block ONLY on `verifiedAt` accounts, pending rows reuse/overwrite, change-email relocates the row (all in `api/user.ts` POST `emailCapture` flag; `captureEmail` in store.ts). Recovery (`api/cron.ts` pending branch): day-0 report-ready email #12 (`reportReadyEmail`) on elapsed `now-emailCapturedAt ≥ 1h` at ANY hour (NOT the 11:00 slot), then finish #9/#10/#11 at local 11:00 by age since capture; every CTA = fresh magic link with `?next=/report` (`createMagicLink(email,ttl,next)`) → verify + land on the interactive **`/report`** route (`ReportPage.tsx`, reuses RevealTeaser + "Continue into the program"). Consent lines (`ConsentLine.tsx`) link EXTERNAL `affina.space/privacy` + `affina.space/terms` (no in-app pages). Emails lowercased end-to-end (user.ts GET+POST, App.signIn) so capture and auth/verify converge on one row. Migration `scripts/add-funnel-cols.mjs`. Verify `?next` sanitized to safe in-app paths only.
- Landing at /.
- Out of v2 scope (SPEC §9): Resend 23-trigger emails, booking calendar, Market Research full mode (needs Exa-vs-Tavily), legal mini-course, viral cards, S3 certificate.
- Smaller: AI role-plays (m3l6/m8l4/m12l4), per-entry Interview Log review, "D + text" outreach buttons, m11l4 three-block treatment (awaiting decision).
- Guide PDF weight: `public/downloads/ai-first-founders-guide.pdf` is ~40 MB (36 pp) — fine for alpha testing, too heavy for a mobile lead-magnet (30–60s on 4G; Hobby bandwidth caps ≈2,400 downloads/mo). Shamil exports a compressed version (<10 MB) from the design tool → swap at the SAME path + deploy (URL unchanged — that's the SPEC_DOCS_LIBRARY §1 replace mechanism). No code changes.
